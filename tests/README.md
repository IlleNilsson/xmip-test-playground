# Tests

Add integration and conformance tests here after the repository responsibility and public contracts are accepted.

The shared verification sequence performs semantic analysis, linting, compilation and linking, then executes tests. No verification is triggered automatically by this template.
